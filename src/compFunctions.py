import fogNode;
import fogUser;
import random


def initalize(k, u):
    nodes = [];
    users = [];
    

def generateDistances(start, end, n):
    lista = [];
    for i in range(n):
        temp = random.randrange(start, end) + random.random();
        lista.append(temp);
    
    return lista